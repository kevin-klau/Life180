service : () -> {
  createUser : (text, text) -> (bool);
  getUserScore : (text) -> (int) query;
  setDecrementUserScore : (text) -> ();
  setIncrementUserScore : (text) -> ();
  verifyUser : (text, text) -> (bool) query;
}
